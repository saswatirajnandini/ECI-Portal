{
  "name": "electiq-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for ElectIQ Election Portal",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix electiq-app && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix electiq-app\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix electiq-app && npm run build --prefix server",
    "test:e2e": "npm run test:e2e --prefix electiq-app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
